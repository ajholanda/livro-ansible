---
- name: Inclui tarefas para a distribuição {{ ansible_facts['distribution'] }}
  ansible.builtin.include_tasks: "{{ ansible_facts['distribution'] | lower }}.yml"
  tags: docker
  when: ansible_facts['distribution'] | lower == 'ubuntu'
