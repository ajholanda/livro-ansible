---
- name: Carrega as tarefas para sistemas da família {{ ansible_os_family }}
  include_tasks:
    file: "{{ item }}"
  with_first_found:
    - files:
      - "{{ ansible_os_family | lower }}.yml"
      skip: true
  tags: webserver