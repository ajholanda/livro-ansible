---
- name: Inclui tarefas para o sistema {{ ansible_facts['system'] }}
  include_tasks:
    file: "{{ item }}"
  with_first_found:
    - files:
      - "{{ ansible_facts['system'] | lower }}.yml"
      skip: true
  tags: remotedesktop
