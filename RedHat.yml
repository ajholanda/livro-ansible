  # RedHat.yml
  - name: Instala ou atualiza o httpd 
    package:
      name: httpd
      state: latest
  - name: Habitita inicialização e inicializa o httpd
    service:
      name: httpd
      enabled: yes
      state: started