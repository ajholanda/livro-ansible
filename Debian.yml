  # Debian.yml
  - name: Instala o apache2
    package:
      name: apache2

  - name: Habitita inicialização e inicializa o apache2
    service:
      name: apache2
      enabled: yes
      state: started
