  # Debian.yml
  - name: Instala o apache2
    package:
      name: apache2

  - name: Habilita inicialização e inicializa o apache2
    service:
      name: apache2
      enabled: yes
      state: started
