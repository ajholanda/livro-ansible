---
- name: Administra servidores web
  hosts: webservers

  tasks:
    - name: Inclui as tarefas a família {{ ansible_facts['os_family'] }}
      ansible.builtin.include_tasks: "tasks/webservers-{{ ansible_facts['os_family'] }}.yml"
