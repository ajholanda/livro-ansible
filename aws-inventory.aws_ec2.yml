plugin: aws_ec2
regions:
  - us-east-1
filters:
  instance-state-name: running
  tag:Environment: development
  tag:Project: web
keyed_groups:
  - key: tags.Environment
    prefix: web
    separator: "_"
